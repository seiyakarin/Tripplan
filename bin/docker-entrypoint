#!/bin/bash -e

# 本番環境でRailsサーバを起動するときだけ seed を流す
if [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ]; then
  RAILS_ENV=production ./bin/rails db:prepare
  RAILS_ENV=production ./bin/rails db:seed
fi

exec "$@"
